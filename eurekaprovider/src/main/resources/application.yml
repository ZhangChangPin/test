server:
  port: 8070

eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8060/eureka/ #注册中心的注册地址

spring:
  application:
    name: service-provider #服务名称--调用的时候根据名称来调用该服务的方法
  datasource:
    url: jdbc:mysql://192.168.2.10:43306/new-qwms?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull
    username: qtools
    password: Qaz123456
    driver-class-name: com.mysql.jdbc.Driver

